import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    logo: {
        width: 100,
        height: 100,
        alignSelf: 'center',
        marginTop: 100
    },
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
})